CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(triangle)

FILE(GLOB SRC_FILES triangle.c)
FILE(GLOB INC_FILES triangle.h)

ADD_DEFINITIONS(-DNO_TIMER)

ADD_LIBRARY(triangle STATIC ${SRC_FILES} ${INC_FILES})
